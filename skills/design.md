---
name: Design
description: Build new UI with the expertise of a senior designer, using the complete local ui.sh design system and workflow.
slash: true
---

# Design

Read `ui-design/SKILL.md`, then follow its **Build** mode for the user's request. Load only the supporting files that workflow identifies. Treat the user's slash-command arguments as the design brief.
